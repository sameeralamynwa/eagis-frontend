import { type MetadataRoute } from "next";

export async function generateBlogsSitemaps(
  appUrl: string,
): Promise<MetadataRoute.Sitemap> {
  // fetch data and return site maps

  return [];
}
